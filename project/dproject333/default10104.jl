     function foo(items) {    var x = "All this is syntax highlighted";    return x;}
          function foo(items) {    var x = "All this is syntax highlighted";    return x;}
               function foo(items) {    var x = "All this is syntax highlighted";    return x;}
                    function foo(items) {    var x = "All this is syntax highlighted";    return x;}
      
function foo(a::Array{Any,1})
    x = a[1]::Int32
    b = x+1
    ...
end

function wifth_named(x; name::Int = 1)
    ...
end

function norm(A)
    if isa(A, Vector)
        return sqrt(real(dot(x,x)))
    elseif isa(A, Matrix)
        return max(svd(A)[2])
    else
        error("norm: invalid argument")
    end
end

 [ 0.25*x[i-1] + 0.5*x[i] + 0.25*x[i+1] for i=2:length(x)-1 ]

A = setindex!(A, X, I_1, I_2, ..., I_n)

function count_heads(n)
    c::Int = 0
    for i=1:n
        c += randbool()
    end
    c
end




ddddddd


      


      


      


      


      


      


      

